name: pioneer-writers

on:
  push:
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to DockerHub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and push backend image
        run: |
          docker build -t allanbinga/murandi-backend:v1.0.0 .
          docker push allanbinga/murandi-backend:v1.0.0

      - name: Build and push frontend image
        run: |
          docker build \
            --build-arg VITE_BACKEND_ENDPOINT=http://murandi-apartments.local/murandi/v1 \
            --build-arg VITE_APP_STRIPE_PUBLISHABLE_KEY=pk_test_51QdrihBCCp07HlmuoHw2wFbIXh53izyTmyQlypfFkp7104wuyeLgSOxuN1klA7jeKdshf5TvkVmJPRWhpYQP1grB00HmapinMr \
            -t allanbinga/murandi-frontend:v1.0.0 ./client
          docker push allanbinga/murandi-frontend:v1.0.0

